{% extends 'base.twig' %}

{% block content%}

    <form>
        <div class="form-group">
            <label for="formGroupExampleInput">Название жанра</label>
            <input value="{{ genre.genreTitle }}" type="text" class="form-control" id="UpdateGenreInput" placeholder="Имя">
        </div>


        <div class="form-group">
            <div id="UpdateGenreButton" data-genre-id="{{  genre.genreID }}" class="btn btn-primary">Обновить</div>
        </div>
        <label id="ResultUpdateGenreLabel"></label>
    </form>

{% endblock %}